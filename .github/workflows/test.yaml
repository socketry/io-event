name: Test

on: [push, pull_request]

permissions:
  contents: read

jobs:
  test:
    name: ${{matrix.ruby}} on ${{matrix.os}}
    runs-on: ${{matrix.os}}-latest
    continue-on-error: ${{matrix.experimental}}
    
    strategy:
      matrix:
        os:
          - ubuntu
          - macos
          - windows
        
        ruby:
          - "3.3"
          - "3.4"
          - "4.0"
        
        experimental: [false]
        
        include:
          - os: ubuntu
            ruby: truffleruby
            experimental: true
          - os: ubuntu
            ruby: jruby
            experimental: true
          - os: ubuntu
            ruby: head
            experimental: true
    
    steps:
    - uses: actions/checkout@v4
    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{matrix.ruby}}
        bundler-cache: true
    
    - name: Install packages (Ubuntu)
      if: matrix.os == 'ubuntu'
      run: sudo apt-get install -y liburing-dev
    
    - name: Run tests
      if: matrix.os != 'windows'
      timeout-minutes: 15
      run: bundle exec bake test
    
    - name: Run tests (Windows)
      if: matrix.os == 'windows'
      timeout-minutes: 15
      # Use bash on Windows so the MSYS2/UCRT64 toolchain is on PATH and
      # shared-library linking against ws2_32 resolves correctly.
      shell: bash
      run: bundle exec bake build && timeout 120s bundle exec sus --verbose
